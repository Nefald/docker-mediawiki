<?php
# This file was automatically generated by the MediaWiki 1.19.1
# installer. If you make manual changes, please keep track in case you
# need to recreate them later.
#
# See includes/DefaultSettings.php for all configurable settings
# and their default values, but don't forget to make changes in _this_
# file, not there.
#
# Further documentation for configuration settings may be found at:
# http://www.mediawiki.org/wiki/Manual:Configuration_settings

# Protect against web entry
if ( !defined( 'MEDIAWIKI' ) ) {
	exit;
}

## Uncomment this to disable output compression
# $wgDisableOutputCompression = true;

$wgSitename      = "{{WIKI_SITENAME}}";
$wgMetaNamespace = "Nefald";

## The URL base path to the directory containing the wiki;
## defaults for all runtime URL paths are based off of this.
## For more information on customizing the URLs please see:
## http://www.mediawiki.org/wiki/Manual:Short_URL
$wgScriptPath       = "/wiki";
$wgArticlePath      = "/wiki/$1";
$wgScriptExtension  = ".php";
$wgUsePathInfo = true;

## The protocol and server name to use in fully-qualified URLs
$wgServer           = "https://nefald.fr";

## The relative URL path to the skins directory
$wgStylePath        = "$wgScriptPath/skins";

## The relative URL path to the logo.  Make sure you change this from the default,
## or else you'll overwrite your logo when you upgrade!
$wgLogo             = "$wgScriptPath/resources/assets/nefald_logo.png";

## $wgReadOnly = "Maintenance en cours, le wiki est en lecture seule.";

## UPO means: this is also a user preference option

$wgEnableEmail      = true;
$wgEnableUserEmail  = true; # UPO

## Email
$wgSMTP = array(
        'host' => '{{SMTP_HOST}}',
        'IDHost' => '{{SMTP_ID_HOST}}',
        'port' => '{{SMTP_PORT}}',
        'username' => '{{SMTP_USERNAME}}',
        'password' => '{{SMTP_PASSWORD}}',
        'auth' => true
);

$wgEmergencyContact = "contactr@nefald.fr";
$wgPasswordSender   = "contact@nefald.fr";

$wgEnotifUserTalk      = true; # UPO
$wgEnotifWatchlist     = true; # UPO
$wgEmailAuthentication = true;

## Database settings
$wgDBtype           = "{{WIKI_MYSQL_TYPE}}";
$wgDBserver         = "{{WIKI_MYSQL_HOST}}";
$wgDBname           = "{{WIKI_MYSQL_DATABASE}}";
$wgDBuser           = "{{WIKI_MYSQL_USER}}";
$wgDBpassword       = "{{WIKI_MYSQL_PASSWORD}}";

# MySQL specific settings
$wgDBprefix         = "{{WIKI_MYSQL_PREFIX}}";

# MySQL table options to use during installation or update
$wgDBTableOptions   = "ENGINE=InnoDB, DEFAULT CHARSET=binary";

# Experimental charset support for MySQL 5.0.
$wgDBmysql5 = false;

## Shared memory settings
$wgEnableParserCache = false;
$wgCachePages = false;

$wgMainCacheType    = CACHE_NONE;
$wgMemCachedServers = array();

## To enable image uploads, make sure the 'images' directory
## is writable, then set this to true:
$wgEnableUploads  = true;
$wgUseImageResize = true;
$wgUseImageMagick = true;
$wgImageMagickConvertCommand = "/usr/bin/convert";  #often /usr/bin/convert
$wgUseInstantCommons = false;

/////////UploadWizard
require_once( "$IP/extensions/UploadWizard/UploadWizard.php" );

// Needed to make UploadWizard work in IE, see bug 39877
#$wgApiFrameOptions = '/usr/bin/convert';

$wgUploadWizardConfig = array(
        'debug' => false,
//        'autoCategory' => 'Téléversé avec UploadWizard',
         'feedbackPage' => 'FeedbackTest2',
         'altUploadForm' => 'Special:UploadWizard',
         'fallbackToAltUploadForm' => false,
         'enableFormData' => true,  # Should FileAPI uploads be used on supported browsers?
         'enableMultipleFiles' => true,
         'enableMultiFileSelect' => true,
         'tutorial' => array('skip' => false)
  );

$wgMimeDetectorCommand = "file -bi";
// Override the default with a bundle of filetypes:
$wgFileExtensions = array( 'ico', 'png', 'gif', 'jpg', 'jpeg', 'ppt', 'pdf', 'psd', 'mp3', 'xls', 'xlsx', 'swf', 'doc','docx', 'odt', 'odc', 'odp', 'ogg', 'odg', 'mpp', 'yml', 'zip', 'svg', 'rar', '7zip', 'gz', 'tar');
# InstantCommons allows wiki to use images from http://commons.wikimedia.org
$wgUseInstantCommons  = true;
$wgStrictFileExtensions = true;
## If you use ImageMagick (or any other shell command) on a
## Linux server, this will need to be set to the name of an
## available UTF-8 locale
$wgShellLocale = "fr_FR.utf8";
$wgMaxAnimatedGifArea = 64;

# Enable subpages in the main namespace
$wgNamespacesWithSubpages[NS_MAIN] = true;

## If you want to use image uploads under safe mode,
## create the directories images/archive, images/thumb and
## images/temp, and make them all writable. Then uncomment
## this, if it's not already uncommented:
#$wgHashedUploadDirectory = false;

## Set $wgCacheDirectory to a writable directory on the web server
## to make your wiki go slightly faster. The directory should not
## be publically accessible from the web.
#$wgCacheDirectory = "$IP/cache";

# Site language code, should be one of the list in ./languages/Names.php
$wgLanguageCode = "{{WIKI_LANG}}";

$wgSecretKey = "{{WIKI_SECRET_KEY}}";

# Site upgrade key. Must be set to a string (default provided) to turn on the
# web installer while LocalSettings.php is in place
$wgUpgradeKey = "5970168fd57f1576";

## Default skin: you can change the default skin. Use the internal symbolic
## names, ie 'standard', 'nostalgia', 'cologneblue', 'monobook', 'vector':
#$wgDefaultSkin = "vector";

#wfLoadSkin( 'Vector' );
$wgHiddenPrefs[] = 'skin';

wfLoadSkin( 'Tweeki' );
$wgDefaultSkin = "tweeki";

//Hide parser
$wgTweekiSkinHideable = array( 'sidebar-right' => true, 'PERSONAL' => true, 'TOOLBOX' => true );

// hide the edit button as well as the subnavigation (containing the toolbox 
// and personal links for editors) for anonymous users
$wgTweekiSkinHideAnon['EDIT-EXT'] = true;  
$wgTweekiSkinHideAnon['subnav'] = false;

//Use VisualEditor with Tweeki
$wgVisualEditorSupportedSkins[] = 'tweeki';

//EditSection Link
$wgTweekiSkinCustomEditSectionLink = true;

//Whether or not to use Bootstrap's tooltips for buttons using the data-toggle="tooltip" attribute.
$wgTweekiSkinUseTooltips = true;
$wgTweekiSkinCustomEditSectionLink = true;

//Whether or not to include the code for Bootstrap's theme (enhanced styling for buttons etc.).
$wgTweekiSkinUseBootstrapTheme = true;

//Whether or not to include Font Awesome to allow the use of icons
$wgTweekiSkinUseAwesome = true;

//If set to false, the icons in the footer will be replaced by text aquivalents.
$wgTweekiSkinFooterIcons = false;

# Remove old skins from skin choice of user preferences
# $wgSkipSkins = array( 'chick', 'cologneblue', 'modern', 'myskin', 'nostalgia', 'simple', 'standard' );

## For attaching licensing metadata to pages, and displaying an
## appropriate copyright notice / icon. GNU Free Documentation
## License and Creative Commons licenses are supported so far.
$wgRightsPage = "Nefald"; # Set to the title of a wiki page that describes your license/copyright
$wgRightsUrl  = "http://creativecommons.org/licenses/by-nc-sa/3.0/";
$wgRightsText = "Creative Commons paternité – non commercial – partage à l’identique";
$wgRightsIcon = "{$wgStylePath}/common/images/cc-by-nc-sa.png";

# Path to the GNU diff3 utility. Used for conflict resolution.
$wgDiff3 = "/usr/bin/diff3";

# Query string length limit for ResourceLoader. You should only set this if
# your web server has a query string length limit (then set it to that limit),
# or if you have suhosin.get.max_value_length set in php.ini (then set it to
# that value)
$wgResourceLoaderMaxQueryLength = -1;



//////GROUPES//////
# The following permissions were set based on your choice in the installer
//Anonymes
$wgGroupPermissions['*']['read']                 = true;
$wgGroupPermissions['*']['edit']                 = false;
$wgGroupPermissions['*']['upload']               = false;
$wgGroupPermissions['*']['createpage']           = false;
$wgGroupPermissions['*']['createtalk']           = false;
$wgGroupPermissions['*']['createaccount']        = true;
$wgGroupPermissions['*']['comment'] = false;

//Utilisateurs
$wgGroupPermissions['user']['move']              = false;
$wgGroupPermissions['user']['move-rootuserpages']= false;
$wgGroupPermissions['user']['move-subpages']     = false;
$wgGroupPermissions['user']['read']              = true;
$wgGroupPermissions['user']['edit']              = false;
$wgGroupPermissions['user']['autoconfirmed']     = false;
$wgGroupPermissions['user']['createpage']        = false;
$wgGroupPermissions['user']['createtalk']        = false;
$wgGroupPermissions['user']['upload']            = false;
$wgGroupPermissions['user']['reupload']          = false;
$wgGroupPermissions['user']['reupload-shared']   = false;
$wgGroupPermissions['user']['movefile']          = false;
$wgGroupPermissions['user']['minoredit']         = false;
$wgGroupPermissions['user']['purge']             = false;
$wgGroupPermissions['user']['comment'] = true;
$wgGroupPermissions['user']['commentlinks'] = true;

//Rédacteurs
$wgGroupPermissions['Rédacteur']['read']         = true;
$wgGroupPermissions['Rédacteur']['edit']         = true;
$wgGroupPermissions['Rédacteur']['createpage']   = true;
$wgGroupPermissions['Rédacteur']['upload']       = true;
$wgGroupPermissions['Rédacteur']['reupload']     = true;
$wgGroupPermissions['Rédacteur']['upload_by_url']   = true;
$wgGroupPermissions['Rédacteur']['createtalk']   = true;
$wgGroupPermissions['Rédacteur']['delete']       = false;
$wgGroupPermissions['Rédacteur']['undelete']       = false;
$wgGroupPermissions['Rédacteur']['move']         = true;
$wgGroupPermissions['Rédacteur']['movefile']     = true;
$wgGroupPermissions['Rédacteur']['minoredit']    = true;
$wgGroupPermissions['Rédacteur']['comment'] = true;
$wgGroupPermissions['Rédacteur']['commentlinks'] = true;

//Staff
$wgGroupPermissions['Staff']['read']         = true;
$wgGroupPermissions['Staff']['edit']         = true;
$wgGroupPermissions['Staff']['createpage']   = true;
$wgGroupPermissions['Staff']['upload']       = true;
$wgGroupPermissions['Staff']['reupload']     = true;
$wgGroupPermissions['Staff']['reupload-shared']   = true;
$wgGroupPermissions['Staff']['upload_by_url']   = true;
$wgGroupPermissions['Staff']['createtalk']   = true;
$wgGroupPermissions['Staff']['delete']       = true;
$wgGroupPermissions['Staff']['undelete']     = true;
$wgGroupPermissions['Staff']['move']         = true;
$wgGroupPermissions['Staff']['movefile']     = true;
$wgGroupPermissions['Staff']['minoredit']    = true;
$wgGroupPermissions['Staff']['checkuser']    = true;
$wgGroupPermissions['Staff']['comment'] = true;
$wgGroupPermissions['Staff']['commentlinks'] = true;
$wgGroupPermissions['Staff']['commentadmin'] = true;


//Sysop
$wgGroupPermissions['sysop']['editaccount'] = true;

///////PROTECTION/////
# add an additional protection level restricting edit/move/etc. to users with the "docwriters" permission
$wgRestrictionLevels[] = 'Staff';
# give the "docwriters" permission to users in the "docwriters" group
$wgGroupPermissions['Staff']['Staff'] = true;
# give the "docwriters" permission to sysops (needed so sysops can apply this protection level to pages)
$wgGroupPermissions['sysop']['Staff'] = true;

# add an additional protection level restricting edit/move/etc. to users with the "docwriters" permission
$wgRestrictionLevels[] = 'Rédacteur';
# give the "docwriters" permission to users in the "docwriters" group
$wgGroupPermissions['Rédacteur']['Rédacteur'] = true;
# give the "docwriters" permission to sysops (needed so sysops can apply this protection level to pages)
$wgGroupPermissions['sysop']['Rédacteur'] = true;

//////EXTENSIONS/////
# Enabled Extensions. Most extensions are enabled by including the base extension file here
# but check specific extension documentation for more details
# The following extensions were automatically enabled:
wfLoadExtension( 'Cite' );
#wfLoadExtension( 'Gadgets' );
wfLoadExtension( 'ImageMap' );
wfLoadExtension( 'InputBox' );
wfLoadExtension( 'Nuke' );
wfLoadExtension( 'ParserFunctions' );
wfLoadExtension( 'Poem' );
wfLoadExtension( 'SyntaxHighlight_GeSHi' );
wfLoadExtension( 'WikiEditor' );
wfLoadExtension( 'ConfirmEdit' );
wfLoadExtension( 'Renameuser' );

# Enables use of WikiEditor by default but still allow users to disable it in preferences
$wgDefaultUserOptions['usebetatoolbar'] = 1;
$wgDefaultUserOptions['usebetatoolbar-cgd'] = 1;

# Displays the Preview and Changes tabs
$wgDefaultUserOptions['wikieditor-preview'] = 1;

# Displays the Publish and Cancel buttons on the top right side
$wgDefaultUserOptions['wikieditor-publish'] = 1;

//AUTORISE A Telecharger du INTERNET
$wgAllowCopyUploads = true;

//email de confirmation à l'inscription
$wgEmailConfirmToEdit = true;

//Preview des licences sur les images
$wgAjaxLicensePreview = true;

//No Title on MainPage
require_once "$IP/extensions/NoTitle/NoTitle.php";
$wgRestrictDisplayTitle = false;

//Titre Article (force la casse ou non)
$wgCapitalLinks = false;

//CreateRedirect
require_once( "$IP/extensions/CreateRedirect/CreateRedirect.php" );

//MCRecipe
require_once "$IP/extensions/MCRecipe/MCRecipe.php";

//DynamicSidebar
wfLoadExtension( 'DynamicSidebar' );


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//# End of automatically generated settings.
//# Add more configuration options below.


/////////EmbedVideo
wfLoadExtension( 'EmbedVideo');

/////////TitleKey
require_once( "$IP/extensions/TitleKey/TitleKey.php" );

/////////FancyBoxThumbs
require_once("$IP/extensions/FancyBoxThumbs/FancyBoxThumbs.php");
$fbtFancyBoxOptions = '{"openEffect":"elastic","closeEffect":"elastic","helpers":{"title":{"type":"inside"}}}';


/////////DynamicPageList
require_once "$IP/extensions/DynamicPageList/DynamicPageList.php";


///////EditAccount
wfLoadExtension( 'EditAccount' );

///////EditCount
#wfLoadExtension( 'Editcount' );


////////////EasyTimeline
require_once "$IP/extensions/timeline/Timeline.php"; // Add EasyTimeline extension
#$fontFile = 'ascii'; // Use the internal Ploticus font
$wgTimelineSettings->ploticusCommand = "/usr/bin/ploticus";
$wgTimelineSettings->perlCommand = "/usr/bin/perl";

putenv("GDFONTPATH=/usr/share/fonts/truetype/freefont");

///// A D M I N I S T R A T I O N /////
/////////Interwiki
require_once "$IP/extensions/Interwiki/Interwiki.php";

/////////UserMerge
wfLoadExtension( 'UserMerge' );
// By default nobody can use this function, enable for bureaucrat?
$wgGroupPermissions['bureaucrat']['usermerge'] = true;

//optional: default is array( 'sysop' )
$wgUserMergeProtectedGroups = array( 'sysop' );


////////CheckUser
wfLoadExtension( 'CheckUser' );
$wgAddGroups['sysop'][] = 'checkuser';


/////////AntiSpoof
require_once( "$IP/extensions/AntiSpoof/AntiSpoof.php" );


////////SpamBlackList
wfLoadExtension( 'SpamBlacklist' );
$wgSpamBlacklistFiles = array(
   "http://fr.wikipedia.org/wiki/MediaWiki:Spam-blacklist",
   "http://en.wikipedia.org/wiki/MediaWiki:Spam-blacklist",
   "http://meta.wikimedia.org/w/index.php?title=Spam_blacklist&action=raw&sb_ver=1", // Wikimedia's list
   //  database      title
   "DB: wikidb MediaWiki:Spam-blacklist",
);

$wgEnableDnsBlacklist = true;
$wgDnsBlacklistUrls = array( 'xbl.spamhaus.org', 'opm.tornevall.org' );


////////QuestyCaptcha
wfLoadExtension( 'ConfirmEdit/QuestyCaptcha' );

$wgCaptchaClass = 'QuestyCaptcha';
$arr = array (
        "Qui a créé Minecraft?" => "notch",
        "Quel est le nom de ce wiki?" => "$wgSitename",
        'Sur Nefald, comment appelle t-on un admin?' => 'intendant',
        'Sur Nefald, comment appelle t-on un modo?' => 'officier',
        'Quel est le nom de notre communauté?' => 'nefald',
        'De quel mob s‘agit-il? <img src="https://nefald.fr/wiki/images/f/ff/Cochon.png" alt="" title="" width="30px"/>' => 'cochon',
        'De quel mob s‘agit-il? <img src="https://nefald.fr/wiki/images/3/33/Poule.png" alt="" title="" width="30px"/>' => 'poule',
);
foreach ( $arr as $key => $value ) {
        $wgCaptchaQuestions[] = array( 'question' => $key, 'answer' => $value );
}

/////////ReplaceText
wfLoadExtension( 'ReplaceText' );

/////////Piwik

require_once "$IP/extensions/Piwik/Piwik.php";
$wgPiwikURL = "stats.hiob.fr";
$wgPiwikIDSite = "2";
